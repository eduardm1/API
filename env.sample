# -----------------------------------------------------------------------------------------------------------------------------
# Step 1: Please enter the credentials of your PostgreSQL database received by the group leader at the beginning of the module.
# -----------------------------------------------------------------------------------------------------------------------------
# NOTE: same credentials used for PgAdmin
# It should look similar to the following:

# POSTGRES_HOST = mod3-postgres.postgres.database.azure.com
# POSTGRES_PORT = 5432
# POSTGRES_USER = mod3_team_2@mod3-postgres
# POSTGRES_PASSWORD = jdaadKjQEgL5SHVDK
# POSTGRES_DB = mod3_db_2
# POSTGRES_SCHEMA = ProjectSchema

POSTGRES_HOST =
POSTGRES_PORT =
POSTGRES_USER =
POSTGRES_PASSWORD =
POSTGRES_DB =
POSTGRES_SCHEMA =

# Don't change this
NODE_ENV = development
DATABASE_URL = postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/${POSTGRES_DB}?schema=${POSTGRES_SCHEMA}&ssl=true

# -----------------------------------------------------------------------------------------------------------------------------
# Step 2: Save the file
# -----------------------------------------------------------------------------------------------------------------------------

# -----------------------------------------------------------------------------------------------------------------------------
# Step 3: Rename this 'env.sample' file to '.env'
# -----------------------------------------------------------------------------------------------------------------------------
